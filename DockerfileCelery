FROM python:3.6

COPY requirements_celery.txt /requirements.txt
RUN pip install -r /requirements.txt

COPY ./backend/celery /celery_tasks
WORKDIR /celery_tasks

ENTRYPOINT celery -A backend.celery.tasks worker --loglevel=info